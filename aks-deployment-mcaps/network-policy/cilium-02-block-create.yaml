## Cilium L7 Policy: Block POST (create) from customer-frontend (pod-to-pod)
## customer-frontend: allow GET, PUT, DELETE, PATCH — block POST
## VNet (10.0.0.0/16): full access (no L7 filtering)
## Requires ACNS: az aks update --name aks-vk-with-cilium --resource-group azure-vk-rg --enable-acns
## Apply:  kubectl apply -f cilium-02-block-create.yaml
## Remove: kubectl delete -f cilium-02-block-create.yaml
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: block-customer-api-create
  namespace: dpworld
spec:
  endpointSelector:
    matchLabels:
      app: customer-api
  ingress:
    # customer-frontend pod-to-pod: allow all EXCEPT POST
    - fromEndpoints:
        - matchLabels:
            app: customer-frontend
      toPorts:
        - ports:
            - port: "5000"
              protocol: TCP
          rules:
            http:
              - method: GET
              - method: PUT
              - method: DELETE
              - method: PATCH
              - method: HEAD
              - method: OPTIONS
    # VNet traffic (AppGW, nodes, APIM): full access
    - fromCIDR:
        - "10.0.0.0/16"
      toPorts:
        - ports:
            - port: "5000"
              protocol: TCP
